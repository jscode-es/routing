import React from 'react';
import { Tabs } from '@jscode/react-native-routing';

// Título e icono de cada pestaña salen del export metadata de su página.
export default function TabsLayout() {
  return <Tabs animation="fade" />;
}
